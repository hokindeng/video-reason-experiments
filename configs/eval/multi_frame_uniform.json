{
  "sampling_strategy": "uniform",
  "inference_dir": "../data/outputs",
  "eval_output_dir": "../data/evaluations/{strategy}/{evaluator}",
  "temperature": 0.0,
  "multiframe": {
    "n_frames": 8,
    "last_seconds": 3.0,
    "strategy": "uniform",
    "voting": "weighted_majority",
    "metric": "histogram",
    "temporal_weight": 0.3,
    "confidence_threshold": 0.6
  },
  "_note": "Evaluator must be specified via --evaluator flag (gpt4o|internvl). Multi-frame evaluation uses uniform sampling."
}
