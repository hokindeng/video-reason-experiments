{
  "sampling_strategy": "hybrid",
  "inference_dir": "../data/outputs",
  "eval_output_dir": "../data/evaluations/{strategy}/{evaluator}",
  "temperature": 0.0,
  "multiframe": {
    "n_frames": 8,
    "last_seconds": 3.0,
    "strategy": "hybrid",
    "voting": "weighted_majority",
    "metric": "combined",
    "temporal_weight": 0.35,
    "confidence_threshold": 0.6
  },
  "_note": "Evaluator must be specified via --evaluator flag (gpt4o|internvl). Multi-frame evaluation uses hybrid sampling."
}
